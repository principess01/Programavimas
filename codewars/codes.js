//Test cases for the function

//Array sum

function sum(numbers) {
    return numbers.reduce((a, b) => a + b, 0);
  }

//Sentence smash
  smash = function (words) {
    return words.join(" ");
  };

  